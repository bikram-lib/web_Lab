10.Write a program to change the text and text color of a paragraph when a button is clicked. The button should toggle between two different texts and colors


as-3

Q1. Write an HTML page including required JavaScript that:
Takes a number from a text field
Accepts numbers only in the range 0 to 999
Displays the number in words
Does not accept:
Four or more digit numbers
Alphabets
Special characters
Q2. Design the following static web pages required for an Online Book Store:
a) Home Page
Must contain three frames
b) Login Page
c) Catalogue Page
Display details of all available books
Use a table to show:
Book Name
Author
Price
d) Registration Page
Q3. Create a web page to display your Class Time Table using:
HTML table
Proper headings for days and periods
Q4. Design the required static web pages for an Online Shopping Cart application.
Q5. Write JavaScript code to validate the following fields in a Registration Page:
First Name
Should contain only alphabets
Minimum length: 6 characters
Password
Minimum length: 6 characters
E-mail ID
Must follow the standard pattern:
name@domain.com
Mobile Number
Must contain exactly 10 digits
Last Name and Address
Should not be empty
Q6. Design a web page using CSS that includes the following:
a) Use different font styles
b) Control image repetition using:
background-repeat
no-repeat
c) Define styles for links:
a:link
a:active
a:hover
a:visited
d) Add customized cursors for links
Bottom of Form




Q7. Create a responsive web page for any one of the following:
College Club
Product Promotion Page
Event Information Page
The page must contain:
Header
Navigation menu
Content section
Footer
Apply CSS3 features:
Rounded corners
Shadow effects
Gradient background
Transition effect
Transform effect





assign-4

1. Write a program to create a dynamic Student List Manager.

User can:

Add student names

Remove a student

Display total number of students

Use:
#DOM createElement()
#appendChild()
#Remove button per item

2. Write a program to generate a random OTP (6-digit) and verify it.

Requirements:

Generate OTP on button click

User enters OTP

Show “Valid OTP” or “Invalid OTP”

Use:
#Math.random()
#DOM validation

3. Write a program to create a simple Calculator with history.

Requirements:

Perform +, –, ×, ÷

Store last 5 calculations on the page

Use:
#Array
#DOM dynamic update

4. Write a program to create a live search filter for a list of items.

Requirements:

Display 10 items in list

As user types, matching items remain visible

Use:
#onkeyup event
#filter()
#DOM hide/show

5. Write a program to build a countdown timer.

Requirements:

User enters seconds

Timer counts down

Show “Time Over” when finished

Use:
#setInterval()
#DOM update

6. Write a program to validate a full Registration Form with multiple rules.

Rules:

Name not empty

Email format correct

Password strong (uppercase + number)

Confirm password match

Use:
#Regex
#Error messages below fields

7. Write a program to store and display user login session using LocalStorage.

Requirements:

Save username

Show “Welcome back” after refresh

Logout clears storage

Use:
#localStorage.setItem()
#removeItem()

8. Write a program to create an Image Slider (Next/Previous).

Requirements:

Display one image at a time

Next/Prev buttons change image

Auto-slide every 3 seconds

Use:
#Array of images
#setInterval()
#DOM src update

9. Write a program to create a Quiz Application.

Requirements:

Display one question with options

User selects answer

Show score at end

Use:
#Objects/Arrays
#DOM dynamic content

10. Write a program to fetch and display JSON data from a file using Fetch API.

Requirements:

Load student JSON file

Display in table

Add search option

Use:
#fetch()
#JSON.parse()
#DOM table

11. Write a program to implement a Dark/Light Theme Toggle with saved preference.

Requirements:

Toggle theme button

Theme saved even after reload

Use:
#classList.toggle()
#localStorage

12. Write a program to create a Real-Time Character Limit Checker.

Requirements:

Textarea max 200 chars

Show remaining characters

Prevent typing after limit

Use:
#input event
#length validation
